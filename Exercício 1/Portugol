Algoritmo de idade

Início do algoritmo
    
    ESCREVAL ("Bom dia, quantos anos você tem?")
    LEIA var idadePessoa: REAL

    int: idadeEmMeses = (idadePessoa * 12)
    int: idadeEmDias = (idadeEmMeses * 30);
    int: idadeEmHoras = (idadeEmDias * 24);
    int: idadeEmMinutos = (idadeEmHoras * 60);
    ESCREVAL ("Legal! Isso significa que...")
    ESCREVAL ("Sua idade, em anos, equivale a " + idadeEmMeses + " meses, " + idadeEmDias + " dias, " + idadeEmHoras + " horas e " + idadeEmMinutos + " minutos.")
    
    ESCREVAL ("Isso é muito tempo!")

Fim